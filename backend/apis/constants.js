const {
    NODE_ENV,
    SERVER_PORT,
    BASE_URL,
    CLIENT_URL,
    OPENAI_API_KEY,
    ELEVENLABS_API_KEY,
  } = process.env;
  
  const envConstants = {
    NODE_ENV,
    SERVER_PORT,
    BASE_URL,
    CLIENT_URL,
    OPENAI_API_KEY,
    ELEVENLABS_API_KEY,
  };
  
  module.exports = envConstants;
  